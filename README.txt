Instruções para executar a API do Jogo da Velha:

1- Instalar Python e Flask (utilizando "pip install Flask" no terminal) na máquina a executar a build.
2- Navegar até a pasta contendo os arquivos da API.
3- Abrir o terminal na pasta e digitar "python api.py".